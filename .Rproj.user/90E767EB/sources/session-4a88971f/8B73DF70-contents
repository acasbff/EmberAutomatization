
# Converted ARIMA Model Analysis in Python

```{python}
import pandas as pd
import numpy as np
from statsmodels.tsa.arima.model import ARIMA
import matplotlib.pyplot as plt

# Load the data
df = pd.read_csv('/mnt/data/monthly_full_release_long_format-4_20240826.csv')

# Filter for Germany
germany_data = df[df['Area'] == 'Germany']

# Convert 'Date' to datetime
germany_data['Date'] = pd.to_datetime(germany_data['Date'])

# Focus on Demand data
demand_data = germany_data[germany_data['Variable'] == 'Demand']

# Set the date as the index
demand_data.set_index('Date', inplace=True)

# Fit the ARIMA model
model = ARIMA(demand_data['Value'], order=(5,1,0))
model_fit = model.fit()

# Print the model summary
print(model_fit.summary())

# Forecast for the next 12 months
forecast = model_fit.forecast(steps=12)

# Plot the results
plt.figure(figsize=(10, 6))
plt.plot(demand_data.index, demand_data['Value'], label='Observed')
plt.plot(forecast.index, forecast, label='Forecast', color='red')
plt.title('ARIMA Model Forecast')
plt.xlabel('Date')
plt.ylabel('Electricity Demand (TWh)')
plt.legend()
plt.grid(True)
plt.tight_layout()

# Save the plot
plt.savefig('/mnt/data/arima_forecast.png')

plt.show()
```
